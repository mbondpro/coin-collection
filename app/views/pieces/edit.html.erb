<h1>Editing piece</h1>

<%= form_for(@piece) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :coin_id %><br />
    <%= f.text_field :coin_id %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @piece %> |
<%= link_to 'Back', pieces_path %>